# Current Progress

- Git repository is initialized on branch `main`.
- CNN-only workspace is organized in `CNN_model/`.
- CNN code is available in `CNN_model/Model_code/`.
- Trained model weights are available in `CNN_model/model_weights/`.
- Dataset is available in `CNN_model/dataset/` with split folders.
- Training logs folder is available in `CNN_model/logs/`.
- CNN-specific documentation is available in `CNN_model/README.md`.
